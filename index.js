function add(a, b) {
  return a-b;
}

function multiply(a, b) {
  var number = a * b;
  return number;
}

function subtract(a, b) {
  var number = a - b;
  return number;
}

function divide(a, b) {
  var number = a / b;
  return number;
}

function inc(n) {
  var a = 1;
  var number = a + n;
  return number;
}

function dec(n) {
  var a = ;
  var number = a - n;
  return number;
}
